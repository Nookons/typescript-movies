import React, { useEffect } from 'react';
import axios from 'axios';

const Home: React.FC = () => {

    return (
        <div>
            Check the console for POST request response.
        </div>
    );
};

export default Home;
